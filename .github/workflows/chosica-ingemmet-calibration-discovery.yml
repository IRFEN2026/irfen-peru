name: IRFEN - Chosica INGEMMET 2015 calibration evidence

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - scripts/discover_chosica_ingemmet_2015.py
      - .github/workflows/chosica-ingemmet-calibration-discovery.yml

permissions:
  contents: write

jobs:
  discover:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - run: pip install requests pypdf
      - run: python scripts/discover_chosica_ingemmet_2015.py
      - run: |
          test -s site/data/calibration/chosica_ingemmet_2015_index.json
          python -m json.tool site/data/calibration/chosica_ingemmet_2015_index.json >/dev/null
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add site/data/calibration/chosica_ingemmet_2015_index.json
          git diff --cached --quiet || git commit -m "IRFEN v0.8: update Chosica INGEMMET calibration index"
          git pull --rebase origin main
          git push
