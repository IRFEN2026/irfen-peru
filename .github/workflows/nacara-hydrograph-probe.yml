name: IRFEN - Probar hidrograma Puente Nacara

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - scripts/probe_nacara_hydrograph.py
      - .github/workflows/nacara-hydrograph-probe.yml

permissions:
  contents: write

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - run: pip install requests
      - run: python scripts/probe_nacara_hydrograph.py
      - run: |
          test -s site/data/hydrology/nacara_hydrograph_probe.json
          python -m json.tool site/data/hydrology/nacara_hydrograph_probe.json >/dev/null
      - name: Guardar resultado
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add site/data/hydrology/nacara_hydrograph_probe.json
          git diff --cached --quiet || git commit -m "IRFEN v0.8: record Puente Nacara hydrograph access result"
          git pull --rebase origin main
          git push
